<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hang.mapper.CommentMapper">

    <insert id="addComment">
        insert into yc_shop.comment(uid, shop_id, root_id, content,create_time)
        value (1,#{dto.shopId},#{dto.id},#{content},null)
    </insert>
</mapper>